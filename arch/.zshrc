# ============================================================================
# Arch Linux - Host-specific Overrides
# ============================================================================
# This file is sourced at the end of .zshrc
# Use for: host-specific aliases, functions, settings

# SSH Agent via gpg-agent
export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/gnupg/S.gpg-agent.ssh"

# Add arch-specific aliases, functions, and settings below
