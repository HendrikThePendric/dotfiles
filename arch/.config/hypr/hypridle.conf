# Hypridle config for Hyprland + Hyprlock

general {
    lock_cmd = hyprlock
    before_sleep_cmd = hyprlock
    after_sleep_cmd = hyprctl dispatch dpms on
}

timeouts {
    # Lock after 5 minutes
    lock = 300
    # Suspend after 10 minutes
    suspend = 600
}

# Add more options as needed
