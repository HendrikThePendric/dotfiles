# Start with autogenerated stuff so my setting take precedence in case of conflict
# BEGIN_KITTY_THEME
# Catppuccin-Mocha
include catppuccin-mocha.conf
# END_KITTY_THEME

allow_remote_control yes
scrollback_lines 10000
confirm_os_window_close 0

# Host-specific settings
# This file should be stored in the host-specific directory (arch/, macos/, ubuntu/)
# and will be symlinked to ~/.config/kitty/kitty.conf.local
include kitty.conf.local
