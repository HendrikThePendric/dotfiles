# ============================================================================
# Local Environment Variables
# ============================================================================
# This file contains machine-specific environment variables and secrets.
# Copy this file to ~/.config/env/env.local and fill in your values.
#
# This file is sourced early in .zshrc (before most other config).
# DO NOT commit the actual env.local file - it should remain private!
# ============================================================================

# ----------------------------------------------------------------------------
# Git Configuration
# ----------------------------------------------------------------------------
# Your git email (personal or work)
export GIT_USER_EMAIL="your@email.com"

# GPG signing key for commit signing (optional)
# Find your key ID with: gpg --list-secret-keys --keyid-format=long
# export GPG_SIGNING_KEY="YOUR_GPG_KEY_ID"

# ----------------------------------------------------------------------------
# API Keys & Tokens
# ----------------------------------------------------------------------------
# GitHub CLI / Copilot
# export GITHUB_API_KEY="ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# DeepSeek AI API
# export DEEPSEEK_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# OpenAI / GitHub Copilot proxy
# export OPENAI_API_BASE="https://api.githubcopilot.com"
# export OPENAI_API_KEY="ghu_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# ----------------------------------------------------------------------------
# NOTE: Machine/host configuration goes elsewhere!
# ----------------------------------------------------------------------------
# For machine-specific settings (paths, tool configs, etc.), use:
#   - <host>/.zshrc.env (e.g., macos/.zshrc.env) for early PATH/env setup
#   - <host>/.zshrc.local (e.g., macos/.zshrc.local) for aliases and late config
#
# Examples that DON'T belong here:
#   - OLLAMA_API_BASE, OLLAMA_NUM_CTX (→ .zshrc.local)
#   - DHIS2_HOME (→ .zshrc.local)
#   - PATH additions (→ .zshrc.env)
#   - ANTIDOTE_DIR (→ .zshrc.env)
